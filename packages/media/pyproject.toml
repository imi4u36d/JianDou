[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jiandou-media"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "jiandou-shared==0.1.0",
]

[tool.setuptools]
packages = ["jiandou_media"]
